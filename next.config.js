/** @type {import('next').NextConfig} */
const nextConfig = {
  // your existing configuration options here, for example:
  reactStrictMode: true,
  // add other options as needed
};

module.exports = nextConfig;
